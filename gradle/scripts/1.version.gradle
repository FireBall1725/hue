apply plugin: "com.limark.gitflowsemver"

versionConfig {
    alphaLabel = "pre"
    rcLabel = "beta"
    gitDescribeMatchRule = "*[0-9].[0-9]*.[0-9]*"
    propertiesFile = "${project.buildDir}/version.properties"
}